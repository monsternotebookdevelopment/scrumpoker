apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monster.fullname" . }}-test-connection"
  labels:
    {{- include "monster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
